package io.fastpix.media3.seekpreview.models

/**
 * Preview display mode for seek bar thumbnails.
 */
enum class PreviewMode {
    /**
     * Display thumbnail images from sprite sheet.
     */
    THUMBNAIL,
    
    /**
     * Display timestamp text (e.g., "MM:SS").
     */
    TIMESTAMP
}
